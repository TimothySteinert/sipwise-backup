# ==========================================================
# Sipwise Backup – Configuration
# ==========================================================

# Instance type: master | dr
instance_type: master
server_name: sipwise-server-01

# ----------------------------------------------------------
# Storage Settings
# ----------------------------------------------------------
storage:
  type: local  # local | remote

  local:
    directory: /opt/sipwise-backup/backups

  remote:
    directory: /backups/sipwise
    username: backupuser
    password: changeme

# ----------------------------------------------------------
# Backup Settings (Master only)
# ----------------------------------------------------------
backup:
  automatic:
    enabled: true
    frequency:
      value: 1
      unit: hours  # minutes | hours

  retention:
    days: 30

  cleanup:
    enabled: true
    mode: last_per_day  # keeps only the last backup per calendar day

# ----------------------------------------------------------
# Sync Settings (DR only)
# ----------------------------------------------------------
sync:
  automatic:
    enabled: true
    frequency:
      value: 1
      unit: days  # minutes | hours | days

# ----------------------------------------------------------
# Reboot Settings
# ----------------------------------------------------------
reboot:
  automatic: disabled  # enabled | disabled
  schedule:
    day_of_month: 1
    time: "03:00"  # 24-hour format HH:MM
